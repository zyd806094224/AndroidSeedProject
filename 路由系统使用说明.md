# 路由系统使用说明

## 🎯 概述

本项目已经实现了完整的外部路由链接跳转功能，支持服务端下发路由链接直接跳转到对应页面。这个系统基于 ARouter 框架，提供了灵活的路由配置和强大的参数传递能力。

## ✨ 功能特性

- ✅ **外部URL支持**: 支持 `seedapp://` 协议的URL跳转
- ✅ **多种路由方式**: 支持Activity、Fragment路由
- ✅ **参数传递**: 支持String、Int、Boolean等多种参数类型
- ✅ **路由拦截**: 提供参数验证和处理机制
- ✅ **回退机制**: 不支持的URL有默认处理逻辑
- ✅ **工具封装**: 提供便捷的路由跳转方法

## 📱 支持的路由页面

### 1. WebView页面
- **路由路径**: `/web/activity`
- **支持参数**:
  - `url`: 网页URL (可选，默认: http://106.15.7.132:3000/)
  - `title`: 页面标题 (可选，默认: 网页)
  - `showShare`: 是否显示分享按钮 (可选，默认: false)

### 2. 编辑页面
- **路由路径**: `/edit/activity`
- **支持参数**:
  - `content`: 默认编辑内容 (可选，默认: 空)
  - `hint`: 输入提示文本 (可选，默认: 请输入内容)

### 3. 主页
- **路由路径**: `/main/activity/home`
- **支持参数**:
  - `tab`: 底部导航索引 (可选，默认: 0，0=首页，1=我的)

## 🔗 URL格式规范

### 基本格式
```
seedapp://[路由路径]?[参数1=值1]&[参数2=值2]
```

### 示例URL

#### 1. 跳转到WebView页面
```bash
# 基本跳转
seedapp://web/activity

# 带参数跳转
seedapp://web/activity?url=https://www.baidu.com&title=百度搜索&showShare=true

# URL编码后的格式
seedapp://web/activity?url=https%3A%2F%2Fwww.baidu.com&title=%E7%99%BE%E5%BA%A6%E6%90%9C%E7%B4%A2
```

#### 2. 跳转到编辑页面
```bash
# 基本跳转
seedapp://edit/activity

# 带参数跳转
seedapp://edit/activity?content=默认内容&hint=请输入评论
```

#### 3. 跳转到主页
```bash
# 基本跳转
seedapp://main/activity/home

# 跳转到我的页面
seedapp://main/activity/home?tab=1
```

## 🛠 使用方法

### 1. 服务端下发路由

服务端可以返回如下格式的路由信息：

```json
{
    "code": 200,
    "data": {
        "routeUrl": "seedapp://web/activity?url=https://example.com&title=活动页面",
        "fallbackUrl": "https://example.com/mobile",
        "deepLink": {
            "android": "seedapp://web/activity?url=https://example.com",
            "ios": "myapp://web?url=https://example.com"
        }
    }
}
```

### 2. 客户端处理路由

#### 方法一：使用工具类
```kotlin
// 跳转到WebView页面
RouteUtils.toWebView(context, "https://www.github.com", "GitHub", true)

// 跳转到编辑页面
RouteUtils.toEdit(context, "默认内容", "请输入文字")

// 跳转到主页
RouteUtils.toMain(context, 1) // 跳转到我的Tab

// 处理外部URL
RouteUtils.handleExternalUrl(context, "seedapp://web/activity?url=https://www.baidu.com")
```

#### 方法二：直接使用RouterService
```kotlin
// 处理外部URL
RouterService.handleExternalRoute(context, "seedapp://web/activity?url=https://example.com")

// 构建路由URL
val url = RouterService.buildRouteUrl(WEB_ACTIVITY, mapOf(
    "url" to "https://www.baidu.com",
    "title" to "百度搜索"
))
```

#### 方法三：直接使用ARouter
```kotlin
ARouter.getInstance()
    .build(WEB_ACTIVITY)
    .withString("url", "https://www.baidu.com")
    .withString("title", "百度搜索")
    .navigation(context)
```

### 3. AndroidManifest配置

已经配置了以下Intent Filter支持外部跳转：

```xml
<activity android:name="com.demo.main.MainActivity" android:exported="true">
    <!-- 支持seedapp://协议 -->
    <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="seedapp" />
    </intent-filter>

    <!-- 支持HTTP/HTTPS协议 -->
    <intent-filter android:autoVerify="true">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="http" />
        <data android:scheme="https" />
        <data android:host="seedapp.com" />
    </intent-filter>
</activity>
```

## 🧪 测试方法

### 1. 使用应用内测试
在"我的"页面提供了测试按钮：
- **跳转WebView页面**: 测试WebView路由跳转
- **跳转编辑页面**: 测试编辑页面路由跳转
- **跳转到主页**: 测试主页路由跳转
- **测试外部URL跳转**: 测试外部URL处理
- **测试所有路由**: 批量测试所有路由功能

### 2. 使用ADB命令测试
```bash
# 测试WebView页面
adb shell am start -W -a android.intent.action.VIEW -d "seedapp://web/activity?url=https://www.baidu.com&title=百度" com.demo.androidseedproject

# 测试编辑页面
adb shell am start -W -a android.intent.action.VIEW -d "seedapp://edit/activity?content=测试内容&hint=请输入" com.demo.androidseedproject

# 测试主页
adb shell am start -W -a android.intent.action.VIEW -d "seedapp://main/activity/home?tab=1" com.demo.androidseedproject
```

### 3. 使用浏览器测试
在手机浏览器中输入以下URL：
```
seedapp://web/activity?url=https://www.baidu.com&title=百度搜索
```

## 🎨 iOS端兼容性

为了保持iOS端兼容，建议使用统一的路由协议：

```json
{
    "route": {
        "type": "external_link",
        "url": "https://example.com",
        "title": "活动页面",
        "fallback": {
            "android": "seedapp://web/activity?url=https://example.com",
            "ios": "myapp://web?url=https://example.com",
            "h5": "https://example.com/mobile"
        }
    }
}
```

## 📋 核心文件说明

### 1. 路由配置文件
- `lib_common/src/main/java/com/demo/common/constant/ARouterPath.kt`: 路由路径常量定义
- `lib_common/src/main/java/com/demo/common/config/RouteConfig.kt`: 路由配置管理

### 2. 路由服务
- `lib_common/src/main/java/com/demo/common/service/RouterService.kt`: 路由服务核心实现
- `lib_common/src/main/java/com/demo/common/utils/RouteUtils.kt`: 路由工具类

### 3. 路由拦截器
- `lib_common/src/main/java/com/demo/common/interceptor/RouteInterceptor.kt`: 路由拦截和参数验证

### 4. 页面路由注解
- `mod_main/src/main/java/com/demo/main/ui/WebViewActivity.kt`: WebView路由实现
- `mod_main/src/main/java/com/demo/main/ui/EditTextActivity.kt`: 编辑页面路由实现
- `mod_main/src/main/java/com/demo/main/MainActivity.kt`: 主页路由处理

## 🚀 扩展指南

### 1. 添加新的路由页面

#### 步骤1：在ARouterPath中添加路由常量
```kotlin
const val NEW_ACTIVITY = "/new/activity"
```

#### 步骤2：为Activity添加Route注解
```kotlin
@Route(path = NEW_ACTIVITY)
class NewActivity : BaseMvvmActivity<ActivityNewBinding, NewViewModel>() {
    @Autowired
    @JvmField
    var param1: String? = null

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        ARouter.getInstance().inject(this)
    }
}
```

#### 步骤3：更新路由配置
```kotlin
// RouteConfig.kt
val ROUTE_MAP = mapOf(
    // ... 现有路由
    "new" to NEW_ACTIVITY
)
```

#### 步骤4：添加参数验证（可选）
```kotlin
// RouteInterceptor.kt
NEW_ACTIVITY -> {
    // 参数验证逻辑
}
```

### 2. 添加新的参数类型

在RouterService中添加新的参数类型处理：
```kotlin
key == "newParam" -> {
    postcard.withCustomObject(key, value) // 自定义对象类型
}
```

## ⚠️ 注意事项

1. **URL编码**: 所有参数值都需要进行URL编码
2. **参数验证**: 必需参数在路由拦截器中会进行验证
3. **回退处理**: 不支持的URL会触发回退逻辑
4. **安全性**: 敏感参数建议加密传输
5. **兼容性**: 保持与iOS端路由协议的一致性

## 🎉 总结

通过这个路由系统，你可以轻松实现：
- 服务端下发路由链接，客户端直接跳转
- 跨平台统一的路由协议
- 灵活的参数传递和验证
- 完善的错误处理和回退机制

现在你的Android应用已经具备了完整的外部路由跳转能力，可以灵活地响应各种业务需求！