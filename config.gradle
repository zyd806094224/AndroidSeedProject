ext {
    //是否单独运行某个module
    isModule = false

    signingConfig = [
            storePassword: '000000dong',
            keyAlias     : 'demo',
            keyPassword  : '000000dong'
    ]

    // 依赖版本已迁移到 gradle/libs.versions.toml，这里只保留工程公共配置
    android = [
            applicationId    : "com.demo.androidseedproject",
            compileSdk       : 34,
            minSdk           : 23,
            targetSdk        : 34,

            versionCode      : 1,
            versionName      : "1.0.1",
    ]
}
